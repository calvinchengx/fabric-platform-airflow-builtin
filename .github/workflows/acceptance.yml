# Does a PUBLISHED fabric-emulator still carry this platform end to end?
#
# CI proves what this repository owns; this proves what it CONSUMES. Until now
# the emulator's release told only `fabric-platform-notebook-pipelines`, so this
# platform's pin moved when somebody remembered, and a release that broke the
# built-in ApacheAirflowJob path would have been found by a person rather than
# by a run.
name: Acceptance

on:
  # Fired by fabric-emulator's release workflow.
  repository_dispatch:
    types: [fabric-emulator-release]
  # THE GUARD. A dispatch that silently fails to fire (revoked token, renamed
  # event type, a workflow auto-disabled after 60 days of inactivity) is
  # indistinguishable from one that fired and passed. The schedule makes that
  # absence loud within a day.
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

jobs:
  verify:
    name: Verify the released emulator end to end
    runs-on: ubuntu-latest
    timeout-minutes: 45
    defaults:
      run:
        working-directory: fabric-platform-airflow-builtin
    # To push the adopted pin back; the repository default is read-only.
    permissions:
      contents: write
    steps:
      # Full history and a usable token: a depth-1 detached HEAD cannot rebase
      # onto a main that moved during the run.
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          path: fabric-platform-airflow-builtin
      # THE VENDORS, declared in contoso-sources and materialised from it.
      - uses: actions/checkout@v7
        with:
          repository: calvinchengx/contoso-sources
          path: contoso-sources
      # THE PRODUCT. This platform carries none: `PRODUCT` is a path, and the
      # default (`tests/fixture-product`) deliberately cannot build, so a run
      # that forgot this would fail naming the missing pyproject.toml.
      - uses: actions/checkout@v7
        with:
          repository: calvinchengx/contoso-data-product-fabric-airflow-builtin
          path: contoso-data-product-fabric-airflow-builtin
      # THE FAMILY'S FIGURES, from the repository that owns them. Not the
      # product and not a dependency of this platform: the last step of this
      # job runs one script out of this checkout to hold the run's snapshot to
      # the numbers every cell must produce.
      #
      # PINNED BY SHA, not tracking main. Core's own rules say a release is the
      # unit of change and nothing tracks main, and a nightly whose expectations
      # can move without a reviewed commit here is a nightly that can be turned
      # green by editing another repository. A SHA is also what this workflow
      # already does for every third-party action. Move it deliberately, and
      # only when the family's numbers move.
      - uses: actions/checkout@v7
        with:
          repository: calvinchengx/contoso-data-product
          ref: 2cf7bae8a1e1f4a8cddd2cd5510d435e16febb3b
          path: contoso-data-product
      - uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: "0.11.32"
      # VERIFY THE RELEASE THAT FIRED, not the pin in the checkout. A dispatch
      # for 0.33.0 that ran against the 0.32.0 in versions.env would report
      # success, in the emulator's own release history, for a release nobody
      # tested.
      #
      # Only on dispatch. The schedule verifies the existing pin, which answers
      # the other question worth asking: does the version we claim to support
      # still work today?
      # WHICH VERSION THIS RUN IS ABOUT.
      #
      # On a dispatch it is the release that fired. On the schedule it is the
      # NEWEST release, resolved here rather than assumed to be the pin.
      #
      # THAT IS WHAT MAKES THE DISPATCH OPTIONAL. A cross-repository dispatch
      # needs a token with write on every platform, and when that token expires
      # or is under-scoped the platforms go quiet with nothing to say so. Polling
      # costs a day of latency and no secret at all, so the dispatch is now an
      # optimisation for immediacy rather than the only way this runs.
      #
      # When the platform is already current, `latest` equals the pin and this
      # behaves exactly as the schedule did before: it answers "does the version
      # we claim to support still work today?".
      - name: Resolve the version to verify
        id: target
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "${{ github.event_name }}" = "repository_dispatch" ]; then
            version="${{ github.event.client_payload.version }}"
            echo "dispatched for $version"
          else
            version=$(gh release view --repo calvinchengx/fabric-emulator \
                        --json tagName -q .tagName | sed 's/^v//')
            echo "newest release is $version"
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"
      - name: Point at that version
        run: |
          python3 scripts/set_release.py "${{ steps.target.outputs.version }}"
          git --no-pager diff --stat versions.env
      # THE BYTES, not just the declaration checked out above. The platform's
      # own `make sources` generates the compose fragment and refuses when the
      # vendors' fixtures are absent -- on the first scheduled run it refused
      # on `_data/contoso-pos/.api-key`, because nothing here had ever
      # materialised the sources repo. The refusal is right: mokapi would
      # otherwise answer every request 200 from the schema, and a medallion
      # built on invented rows is worse than a red. The two sibling platforms
      # that made this split first carry the same step.
      - name: Materialise the vendor exports
        run: make sources
        working-directory: contoso-sources
      - run: make up PRODUCT=../contoso-data-product-fabric-airflow-builtin SOURCES=../contoso-sources
      - run: make witness PRODUCT=../contoso-data-product-fabric-airflow-builtin SOURCES=../contoso-sources
      # THE NUMBERS, not merely the run (G50). Everything above proves Fabric's
      # own Airflow ran the DAG green; this is the only step that can fail
      # because gold returned different money. The DAG publishes the snapshot to
      # OneLake and `witness.py` fetches it here by filename; until now nothing
      # read it back.
      #
      # IT ALSO PROMOTES witness.py's WARNING TO A FAILURE. That script prints
      # "the run passed but no product_snapshot.json was found" and exits 0,
      # which is the right call there -- the run genuinely passed -- but from
      # this job a cell that published nothing is a cell that proved nothing
      # about the product, and the file's absence now fails.
      #
      # No `if: always()`. A failed run has no numbers worth checking.
      - name: Assert the figures core publishes
        run: |
          python3 ../contoso-data-product/scripts/assert_snapshot.py \
            --label fabric-platform-airflow-builtin \
            product_snapshot.json
      # ADOPT WHAT THIS RUN JUST PROVED. The pin has been verified end to end by
      # here and is sitting uncommitted; discarding it and asking a human to
      # retype it is the only thing between a verified release and an adopted one.
      #
      # No `if: always()`. A step with no condition runs only when everything
      # before it passed, which is the gate wanted: a red run leaves the pin.
      # ADOPT WHENEVER THE PIN MOVED, not only on a dispatch. A scheduled run
      # that verified a newer release end to end has proved exactly what a
      # dispatched one proves, and throwing that away would mean the pin only
      # ever advances when a cross-repository token happens to be working.
      - name: Adopt the version this run just verified
        env:
          VERSION: ${{ steps.target.outputs.version }}
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git diff --quiet versions.env; then
            echo "already pinned at $VERSION; nothing to adopt"
            exit 0
          fi
          git commit -qm "pin $VERSION, adopted by the acceptance run that verified it" versions.env
          git pull --rebase --quiet origin main
          git push origin HEAD:main
          echo "adopted $VERSION" >> "$GITHUB_STEP_SUMMARY"
      - if: always()
        run: make down
